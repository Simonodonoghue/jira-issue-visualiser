{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "attach",
            "name": "Launch Chrome",
            "url": "localhost:3000",
            "port": 3000,
            "webRoot": "${workspaceFolder}"
        }
    ]
}